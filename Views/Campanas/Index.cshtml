@model IReadOnlyList<Practica_1.Models.Campana>

@{
    ViewData["Title"] = "Listado de campañas";
}

<h1>@ViewData["Title"]</h1>

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Categoría</th>
            <th>Estado</th>
            <th>Canal</th>
            <th>Descuento</th>
            <th>Fechas</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var campana in Model)
        {
            <tr>
                <td>@campana.Nombre</td>
                <td>@campana.Categoria</td>
                <td>@campana.Estado</td>
                <td>@campana.Canal</td>
                <td>@campana.DescuentoPct.ToString("P0")</td>
                <td>@campana.FechaInicio.ToString("dd/MM/yyyy") - @campana.FechaFin.ToString("dd/MM/yyyy")</td>
                <td>
                    <a asp-controller="Campanas" asp-action="Detalle" asp-route-id="@campana.Id" class="btn btn-sm btn-primary">
                        Ver detalle
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
